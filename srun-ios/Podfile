platform :ios, '9.0'
use_frameworks!
target ‘srun-ios’ do
  # pod 'SnapKit', '~> 3.0'
  # pod "SwiftSpinner"
    pod 'Reveal-SDK', :configurations => ['Debug']
    pod 'ReachabilitySwift'
end
target 'SrunKit' do 
	pod 'Alamofire', '~> 4.4'
end